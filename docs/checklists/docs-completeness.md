# Docs Completeness Checklist

1. New user can clone project and run `npm install` and `npm start` within 5 minutes.
2. New user can load extension in developer mode from instructions only.
3. New user can submit one successful capture.
4. README troubleshooting covers token mismatch and local host configuration.
5. Runbook includes offline fallback (`Copy JSON`) verification.
